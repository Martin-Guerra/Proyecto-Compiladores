package Lexer;

public class ClaseRobado {

	/*public Token getToken(String fuente){
	this.setEst(0);
	char c;
	while (this.getEst()!=fin && this.pos< fuente.length()){
		if(this.getEst()==-1)
			this.setEst(0);
		c = fuente.charAt(this.pos);
		int col = this.columna(c);
		acciones[this.getEst()][col].realizarAccionSemantica(c, this);
	}
	this.setBuffer("");
	Token aux = new Token(this.token.getToken());
	aux.setLexema(this.token.getLexema());
	this.token.setToken(EOF);
	this.token.setLexema("");
	tokens.add(aux);
return aux;
}*/
}
